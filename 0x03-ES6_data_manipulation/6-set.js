/**
 * Function to create a Set from an array
 * @param {Array} array Input array
 * @returns {Set} Set containing unique elements from the array
 */
function setFromArray(array) {
	return new Set(array);
}

export default setFromArray;
